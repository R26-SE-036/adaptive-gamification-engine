/**
 * Seeds the CodeFix question bank: 5 per error type x 15 error types = 75 -
 * exactly one at each of the five difficulty levels.
 *
 *     node data/seed_codefix_questions.js
 *
 * Idempotent - questions are upserted by id, so re-running replaces rather than
 * duplicates.
 *
 * ============================== WHY CodeFix ==============================
 * The other three games ask the student to RECOGNISE (Bug Hunt: which line is
 * wrong), ORDER (Drag & Drop) or PREDICT (Code Trace). None asks them to write
 * Java. CodeFix shows the same broken code and asks for the corrected line.
 *
 * That is a harder task in the way that matters - a student can point at a
 * broken loop bound without being able to say what it should have been - and it
 * is what makes a real error count possible, because typing an answer means
 * checking it, and every check is graded and recorded (POST /game/check).
 *
 * ========================== HOW ANSWERS ARE MARKED =========================
 * `correctAnswer` is an ARRAY of accepted lines. Whitespace outside string
 * literals is insignificant, so `i<n` and `i < n` both pass; everything else is
 * significant. Several entries are listed wherever a fix has more than one
 * honest form - `i < arr.length` and `i <= arr.length - 1` are both right, and
 * a game that accepted only the author's phrasing would be marking style.
 *
 * See services/gradingService.js and its tests for the exact rule.
 *
 * ======================== A NOTE ON COVERAGE ========================
 * One question per error type at each of the five levels, so every one of
 * the 14 concepts gains a second game type. That matters beyond this game:
 * CONCEPT_GAME_MAPPING currently fixes one type per concept, which is why the
 * next-game recommendation can only really choose a concept (see
 * services/recommendationService.js). This is the first half of undoing that.
 */

const mongoose = require('mongoose');
const path = require('path');
require('dotenv').config({ path: path.join(__dirname, '../.env') });
require('../config/dns').applyDnsOverride();

const QuestionBank = require('../models/QuestionBank');

/**
 * @param {string} id
 * @param {string} errorType
 * @param {string} conceptTag
 * @param {'Beginner'|'Elementary'|'Intermediate'|'Advanced'|'Expert'} difficulty
 * @param {string[]} codeLines
 * @param {number} buggyLineIndex   the line the student must rewrite
 * @param {string[]} correctAnswer  every accepted form of the fixed line
 * @param {string[]} hints          revealed one per wrong attempt, in order
 * @param {string} explanation
 */
const q = (id, errorType, conceptTag, difficulty, codeLines, buggyLineIndex, correctAnswer, hints, explanation) => ({
    id, errorType, conceptTag, difficulty, gameType: 'CodeFix',
    codeLines, buggyLineIndex, correctAnswer, hints, explanation
});

const QUESTIONS = [
    /* ── OFF_BY_ONE_LOOP_BOUNDARY / loop_boundaries ─────────────────────── */
    q('qf_off_by_one_01', 'OFF_BY_ONE_LOOP_BOUNDARY', 'loop_boundaries', 'Beginner',
        ['public class Main {',
         '    public static void printArray(int[] arr) {',
         '        for (int i = 0; i <= arr.length; i++) {',
         '            System.out.println(arr[i]);',
         '        }',
         '    }',
         '}'],
        2,
        ['for (int i = 0; i < arr.length; i++) {',
         'for (int i = 0; i <= arr.length - 1; i++) {'],
        ['The last valid index of an array is length - 1.',
         'With <=, the loop runs one time too many.',
         'Change <= to < , keeping everything else the same.'],
        'i <= arr.length reads arr[arr.length], which is out of bounds. The last valid index is arr.length - 1.'),

    q('qf_off_by_one_02', 'OFF_BY_ONE_LOOP_BOUNDARY', 'loop_boundaries', 'Intermediate',
        ['int[] values = {4, 8, 15, 16, 23};',
         'int total = 0;',
         'for (int i = 1; i < values.length; i++) {',
         '    total += values[i];',
         '}',
         'System.out.println(total);'],
        2,
        ['for (int i = 0; i < values.length; i++) {'],
        ['Which element never gets added to the total?',
         'Array indices start at 0, not 1.',
         'The loop should start at 0.'],
        'Starting at i = 1 silently skips values[0], so the total is 4 short. This one does not crash, which makes it harder to spot.'),

    q('qf_off_by_one_03', 'OFF_BY_ONE_LOOP_BOUNDARY', 'loop_boundaries', 'Advanced',
        ['public static int lastIndexOf(int[] arr, int target) {',
         '    for (int i = arr.length; i >= 0; i--) {',
         '        if (arr[i] == target) {',
         '            return i;',
         '        }',
         '    }',
         '    return -1;',
         '}'],
        1,
        ['for (int i = arr.length - 1; i >= 0; i--) {'],
        ['The loop counts down. Where should a downward loop start?',
         'arr[arr.length] does not exist.',
         'Start one below the length.'],
        'A downward loop must start at arr.length - 1. Starting at arr.length reads past the end on the very first iteration.'),

    /* ── INCORRECT_CONDITIONAL_OPERATOR / conditional_logic ─────────────── */
    q('qf_cond_op_01', 'INCORRECT_CONDITIONAL_OPERATOR', 'conditional_logic', 'Beginner',
        ['int score = 55;',
         'if (score = 60) {',
         '    System.out.println("Pass");',
         '}'],
        1,
        ['if (score == 60) {'],
        ['= assigns a value. What compares two values?',
         'This line changes score instead of testing it.',
         'Use == for comparison.'],
        'A single = assigns rather than compares. In Java this is a compile error for an int; in a boolean context it would silently assign.'),

    q('qf_cond_op_02', 'INCORRECT_CONDITIONAL_OPERATOR', 'conditional_logic', 'Intermediate',
        ['int age = 18;',
         'boolean allowed;',
         'if (age > 18) {',
         '    allowed = true;',
         '} else {',
         '    allowed = false;',
         '}'],
        2,
        ['if (age >= 18) {'],
        ['Should someone who is exactly 18 be allowed?',
         '> excludes the boundary value.',
         'Use >= so that 18 itself passes.'],
        'With >, an age of exactly 18 falls to the else branch. The boundary case is the whole question here.'),

    q('qf_cond_op_03', 'INCORRECT_CONDITIONAL_OPERATOR', 'conditional_logic', 'Advanced',
        ['public static boolean inRange(int n) {',
         '    if (n > 0 || n < 100) {',
         '        return true;',
         '    }',
         '    return false;',
         '}'],
        1,
        ['if (n > 0 && n < 100) {'],
        ['Try n = 500. Which half of the condition is true?',
         'With ||, only one side has to hold.',
         'Both bounds must hold at once.'],
        'With ||, every integer passes: 500 fails n < 100 but satisfies n > 0. A range check needs &&.'),

    /* ── ARRAY_LENGTH_INDEX_MISUSE / array_indexing ─────────────────────── */
    q('qf_arr_len_01', 'ARRAY_LENGTH_INDEX_MISUSE', 'array_indexing', 'Beginner',
        ['int[] nums = {10, 20, 30};',
         'System.out.println(nums[nums.length]);'],
        1,
        ['System.out.println(nums[nums.length - 1]);'],
        ['How many elements are there, and what is the last index?',
         'length is 3, but the valid indices are 0, 1, 2.',
         'Subtract one from length.'],
        'nums[3] does not exist. The last element of a length-3 array is at index 2.'),

    q('qf_arr_len_02', 'ARRAY_LENGTH_INDEX_MISUSE', 'array_indexing', 'Intermediate',
        ['String[] names = {"Ana", "Ben", "Cara"};',
         'for (int i = 0; i < names.length; i++) {',
         '    System.out.println(names[i + 1]);',
         '}'],
        2,
        ['System.out.println(names[i]);'],
        ['On the last iteration, what is i + 1?',
         'i reaches length - 1, so i + 1 reaches length.',
         'Print the element at i itself.'],
        'i + 1 runs past the end on the final iteration, and skips the first name on the way.'),

    q('qf_arr_len_03', 'ARRAY_LENGTH_INDEX_MISUSE', 'array_indexing', 'Advanced',
        ['public static int[] copyOf(int[] source) {',
         '    int[] result = new int[source.length - 1];',
         '    for (int i = 0; i < source.length; i++) {',
         '        result[i] = source[i];',
         '    }',
         '    return result;',
         '}'],
        1,
        ['int[] result = new int[source.length];'],
        ['The loop copies every element. How many will it write?',
         'The destination is one slot too small.',
         'Allocate exactly source.length.'],
        'The array is allocated one element short, so the final copy writes out of bounds. The loop is right; the allocation is wrong.'),

    /* ── STRING_EQUALITY_WITH_OPERATOR / string_comparison ──────────────── */
    q('qf_str_eq_01', 'STRING_EQUALITY_WITH_OPERATOR', 'string_comparison', 'Beginner',
        ['String answer = scanner.nextLine();',
         'if (answer == "yes") {',
         '    System.out.println("Confirmed");',
         '}'],
        1,
        ['if (answer.equals("yes")) {',
         'if ("yes".equals(answer)) {'],
        ['== compares references, not the characters.',
         'Two Strings holding the same text can still be different objects.',
         'Use .equals() instead.'],
        '== asks whether the two references point at the same object. A String read at runtime is a different object even when the text matches.'),

    q('qf_str_eq_02', 'STRING_EQUALITY_WITH_OPERATOR', 'string_comparison', 'Intermediate',
        ['String a = new String("code");',
         'String b = new String("code");',
         'boolean same = (a == b);',
         'System.out.println(same);'],
        2,
        ['boolean same = a.equals(b);',
         'boolean same = (a.equals(b));'],
        ['new String() always creates a fresh object.',
         'a and b hold identical text in two different objects.',
         'Compare contents with .equals().'],
        'new String() defeats the string pool, so a and b are distinct objects and == is false despite identical text.'),

    q('qf_str_eq_03', 'STRING_EQUALITY_WITH_OPERATOR', 'string_comparison', 'Advanced',
        ['public static boolean isCommand(String input) {',
         '    return input != "quit";',
         '}'],
        1,
        ['return !input.equals("quit");',
         'return !"quit".equals(input);'],
        ['The bug is the comparison, not the negation.',
         '!= on Strings compares references too.',
         'Negate an .equals() call.'],
        '!= has the same defect as ==. Negating a reference comparison gives a method that is almost always true regardless of the text.'),

    /* ── LOOP_UPDATE_WRONG_DIRECTION / loop_control ─────────────────────── */
    q('qf_loop_dir_01', 'LOOP_UPDATE_WRONG_DIRECTION', 'loop_control', 'Beginner',
        ['for (int i = 0; i < 5; i--) {',
         '    System.out.println(i);',
         '}'],
        0,
        ['for (int i = 0; i < 5; i++) {'],
        ['Does i ever get closer to 5?',
         'i-- moves away from the stopping condition.',
         'Count up, not down.'],
        'i starts at 0 and decreases, so i < 5 is always true and the loop never ends.'),

    q('qf_loop_dir_02', 'LOOP_UPDATE_WRONG_DIRECTION', 'loop_control', 'Intermediate',
        ['int countdown = 10;',
         'while (countdown > 0) {',
         '    System.out.println(countdown);',
         '    countdown++;',
         '}'],
        3,
        ['countdown--;',
         'countdown -= 1;',
         'countdown = countdown - 1;'],
        ['The condition waits for countdown to reach 0.',
         'Increasing it moves the wrong way.',
         'Decrement instead.'],
        'The loop is a countdown but the variable counts up, so it never reaches 0.'),

    q('qf_loop_dir_03', 'LOOP_UPDATE_WRONG_DIRECTION', 'loop_control', 'Advanced',
        ['for (int i = 10; i >= 0; i += 2) {',
         '    System.out.println(i);',
         '}'],
        0,
        ['for (int i = 10; i >= 0; i -= 2) {'],
        ['The condition stops when i drops below 0.',
         'i += 2 pushes i further from the exit.',
         'Step downward by 2.'],
        'The condition counts down but the step counts up, so i >= 0 never fails.'),

    /* ── UNREACHABLE_CODE_AFTER_RETURN / control_flow ───────────────────── */
    q('qf_unreach_01', 'UNREACHABLE_CODE_AFTER_RETURN', 'control_flow', 'Beginner',
        ['public static int doubled(int n) {',
         '    return n * 2;',
         '    System.out.println("done");',
         '}'],
        2,
        ['}'],
        ['What runs after a return statement?',
         'Nothing below a return can execute.',
         'The line should be removed - close the method instead.'],
        'Code after return is unreachable and Java refuses to compile it. Removing the line is the fix.'),

    q('qf_unreach_02', 'UNREACHABLE_CODE_AFTER_RETURN', 'control_flow', 'Intermediate',
        ['public static String grade(int mark) {',
         '    if (mark >= 50) {',
         '        return "Pass";',
         '        mark = 0;',
         '    }',
         '    return "Fail";',
         '}'],
        3,
        ['}'],
        ['The assignment sits directly after a return.',
         'It can never run.',
         'Delete it and close the if block.'],
        'mark = 0 follows a return inside the same block, so it is unreachable.'),

    q('qf_unreach_03', 'UNREACHABLE_CODE_AFTER_RETURN', 'control_flow', 'Advanced',
        ['public static int firstNegative(int[] arr) {',
         '    for (int i = 0; i < arr.length; i++) {',
         '        return arr[i] < 0 ? i : -1;',
         '    }',
         '    return -1;',
         '}'],
        2,
        ['if (arr[i] < 0) { return i; }',
         'if (arr[i] < 0) return i;'],
        ['How many iterations does this loop actually complete?',
         'Returning on every path ends the loop after one element.',
         'Only return when the element is actually negative.'],
        'Returning unconditionally makes the loop run exactly once, so only arr[0] is ever examined. The trailing return -1 handles the not-found case.'),

    /* ── MISSING_BREAK_IN_SWITCH / switch_statements ────────────────────── */
    q('qf_switch_01', 'MISSING_BREAK_IN_SWITCH', 'switch_statements', 'Beginner',
        ['switch (day) {',
         '    case 1:',
         '        System.out.println("Monday");',
         '    case 2:',
         '        System.out.println("Tuesday");',
         '        break;',
         '}'],
        2,
        ['System.out.println("Monday"); break;'],
        ['With day = 1, what gets printed?',
         'Without break, execution falls into the next case.',
         'End the case with break.'],
        'Case 1 falls through to case 2, so day = 1 prints both Monday and Tuesday.'),

    q('qf_switch_02', 'MISSING_BREAK_IN_SWITCH', 'switch_statements', 'Intermediate',
        ['switch (grade) {',
         '    case "A":',
         '        points = 4;',
         '    case "B":',
         '        points = 3;',
         '        break;',
         '    default:',
         '        points = 0;',
         '}'],
        2,
        ['points = 4; break;'],
        ['An "A" sets points twice. Which value survives?',
         'Fall-through overwrites the first assignment.',
         'Break after setting 4.'],
        'An "A" sets points to 4 and then immediately to 3, because case "A" falls through into case "B".'),

    q('qf_switch_03', 'MISSING_BREAK_IN_SWITCH', 'switch_statements', 'Advanced',
        ['switch (command) {',
         '    case "save":',
         '        save();',
         '    case "quit":',
         '        quit();',
         '        break;',
         '    default:',
         '        help();',
         '}'],
        2,
        ['save(); break;'],
        ['What happens when the user types "save"?',
         'The program saves and then quits.',
         'Break after save().'],
        'Saving falls through into quit(), so every save also exits the program. Fall-through bugs are worst when the next case has a side effect.'),

    /* ── EMPTY_CONDITIONAL_BODY / statement_structure ───────────────────── */
    q('qf_empty_if_01', 'EMPTY_CONDITIONAL_BODY', 'statement_structure', 'Beginner',
        ['if (count > 10);',
         '{',
         '    System.out.println("Too many");',
         '}'],
        0,
        ['if (count > 10)',
         'if (count > 10) {'],
        ['Look carefully at the end of the if line.',
         'A semicolon there ends the if statement immediately.',
         'Remove the semicolon.'],
        'The stray semicolon is the entire body of the if. The block below always runs, whatever count is.'),

    q('qf_empty_if_02', 'EMPTY_CONDITIONAL_BODY', 'statement_structure', 'Intermediate',
        ['for (int i = 0; i < 3; i++);',
         '{',
         '    System.out.println(i);',
         '}'],
        0,
        ['for (int i = 0; i < 3; i++)',
         'for (int i = 0; i < 3; i++) {'],
        ['The loop finishes before the block is reached.',
         'A semicolon after the header makes an empty loop body.',
         'Remove the semicolon.'],
        'The loop spins three times doing nothing, then the block runs once - and i is out of scope by then.'),

    q('qf_empty_if_03', 'EMPTY_CONDITIONAL_BODY', 'statement_structure', 'Advanced',
        ['if (user != null)',
         '    log("found");',
         '    process(user);'],
        2,
        ['{ log("found"); process(user); }'],
        ['Indentation is not what groups statements in Java.',
         'Only the first statement belongs to the if.',
         'Wrap both statements in braces.'],
        'Without braces the if governs only log(). process(user) always runs, which is a NullPointerException when user is null.'),

    /* ── SELF_ASSIGNMENT / assignment_logic ─────────────────────────────── */
    q('qf_self_assign_01', 'SELF_ASSIGNMENT', 'assignment_logic', 'Beginner',
        ['int total = 5;',
         'total = total;',
         'System.out.println(total);'],
        1,
        ['total = total + 5;',
         'total += 5;'],
        ['What does this line change?',
         'Assigning a variable to itself does nothing.',
         'The value has to actually change.'],
        'total = total is a no-op. The line was meant to modify the value.'),

    q('qf_self_assign_02', 'SELF_ASSIGNMENT', 'assignment_logic', 'Intermediate',
        ['public void setName(String name) {',
         '    name = name;',
         '}'],
        1,
        ['this.name = name;'],
        ['Which name is on each side?',
         'The parameter shadows the field.',
         'Qualify the field with this.'],
        'Both sides refer to the parameter, so the field is never written. this.name disambiguates.'),

    q('qf_self_assign_03', 'SELF_ASSIGNMENT', 'assignment_logic', 'Advanced',
        ['int a = 1;',
         'int b = 2;',
         'int temp = a;',
         'a = b;',
         'b = b;'],
        4,
        ['b = temp;'],
        ['This is a swap. Did both values move?',
         'The last line loses the original a.',
         'The saved value is in temp.'],
        'The swap saves a into temp and then never uses it, so a is lost and b is unchanged.'),

    /* ── ALWAYS_TRUE_OR_CONDITION / boolean_logic ───────────────────────── */
    q('qf_bool_or_01', 'ALWAYS_TRUE_OR_CONDITION', 'boolean_logic', 'Beginner',
        ['if (x == 1 || x == 1) {',
         '    System.out.println("one");',
         '}'],
        0,
        ['if (x == 1) {'],
        ['Both sides of the || are identical.',
         'The duplicate adds nothing.',
         'Keep a single test.'],
        'A duplicated condition is redundant. Either the second test was meant to be different, or it should go.'),

    q('qf_bool_or_02', 'ALWAYS_TRUE_OR_CONDITION', 'boolean_logic', 'Intermediate',
        ['boolean valid = (day >= 1) || (day <= 31);',
         'System.out.println(valid);'],
        0,
        ['boolean valid = (day >= 1) && (day <= 31);'],
        ['Try day = 500.',
         'Any number satisfies at least one side.',
         'Both bounds must hold.'],
        'With ||, valid is true for every integer. A range test needs &&.'),

    q('qf_bool_or_03', 'ALWAYS_TRUE_OR_CONDITION', 'boolean_logic', 'Advanced',
        ['public static boolean isWeekend(String d) {',
         '    return d.equals("Sat") || d != "Sun";',
         '}'],
        1,
        ['return d.equals("Sat") || d.equals("Sun");'],
        ['What does the second half return for "Mon"?',
         '!= on a String is nearly always true.',
         'Both halves should be .equals() tests.'],
        'd != "Sun" is a reference comparison that is true for almost every input, so the method reports every day as a weekend.'),

    /* ── IGNORED_STRING_METHOD_RESULT / immutable_strings ───────────────── */
    q('qf_immut_01', 'IGNORED_STRING_METHOD_RESULT', 'immutable_strings', 'Beginner',
        ['String name = "  ana  ";',
         'name.trim();',
         'System.out.println("[" + name + "]");'],
        1,
        ['name = name.trim();'],
        ['Strings cannot be changed in place.',
         'trim() returns a new String.',
         'Assign the result back.'],
        'String is immutable, so trim() returns a new value and leaves name untouched. The result has to be assigned.'),

    q('qf_immut_02', 'IGNORED_STRING_METHOD_RESULT', 'immutable_strings', 'Intermediate',
        ['String code = "abc";',
         'code.toUpperCase();',
         'code.concat("-1");',
         'System.out.println(code);'],
        1,
        ['code = code.toUpperCase();'],
        ['Neither call changes code.',
         'Every String method returns a new String.',
         'Assign the result of toUpperCase back to code.'],
        'Both calls are discarded. This question fixes the first; the same mistake is on the next line.'),

    q('qf_immut_03', 'IGNORED_STRING_METHOD_RESULT', 'immutable_strings', 'Advanced',
        ['public static String clean(String input) {',
         '    input.replace(" ", "");',
         '    return input;',
         '}'],
        1,
        ['input = input.replace(" ", "");',
         'return input.replace(" ", "");'],
        ['The method returns its argument unchanged.',
         'replace() produces a new String.',
         'Either assign it back or return it directly.'],
        'The replacement is computed and thrown away, so clean() is an expensive way of returning its input.'),

    /* ── DIVISION_BY_ZERO_LITERAL / arithmetic_operations ───────────────── */
    q('qf_div_zero_01', 'DIVISION_BY_ZERO_LITERAL', 'arithmetic_operations', 'Beginner',
        ['int total = 100;',
         'int average = total / 0;',
         'System.out.println(average);'],
        1,
        ['int average = total / 1;'],
        ['Integer division by zero throws at runtime.',
         'The divisor must not be 0.',
         'Divide by a non-zero value.'],
        'total / 0 throws ArithmeticException. Integer division has no infinity to fall back on.'),

    q('qf_div_zero_02', 'DIVISION_BY_ZERO_LITERAL', 'arithmetic_operations', 'Intermediate',
        ['int sum = 0;',
         'int count = 0;',
         'for (int n : values) { sum += n; count++; }',
         'System.out.println(sum / count);'],
        3,
        ['if (count > 0) { System.out.println(sum / count); }',
         'System.out.println(count > 0 ? sum / count : 0);'],
        ['What if values is empty?',
         'count stays 0 and the division throws.',
         'Guard the division.'],
        'An empty input leaves count at 0. The average has to be guarded, not assumed.'),

    q('qf_div_zero_03', 'DIVISION_BY_ZERO_LITERAL', 'arithmetic_operations', 'Advanced',
        ['public static int rate(int hits, int attempts) {',
         '    return (hits * 100) / (attempts - attempts);',
         '}'],
        1,
        ['return (hits * 100) / attempts;'],
        ['Work out attempts - attempts for any value.',
         'That expression is always 0.',
         'Divide by attempts itself.'],
        'attempts - attempts is zero for every input, so the method always throws. Subtle because there is no literal 0 on the line.'),

    /* ── CONSTANT_FALSE_LOOP_CONDITION / loop_initialization ────────────── */
    q('qf_const_false_01', 'CONSTANT_FALSE_LOOP_CONDITION', 'loop_initialization', 'Beginner',
        ['for (int i = 0; false; i++) {',
         '    System.out.println(i);',
         '}'],
        0,
        ['for (int i = 0; i < 10; i++) {'],
        ['How many times does this body run?',
         'A literal false means the body never executes.',
         'Give it a real condition.'],
        'The condition is a constant false, so the loop body is dead code.'),

    q('qf_const_false_02', 'CONSTANT_FALSE_LOOP_CONDITION', 'loop_initialization', 'Intermediate',
        ['int i = 10;',
         'while (i < 5) {',
         '    System.out.println(i);',
         '    i++;',
         '}'],
        0,
        ['int i = 0;'],
        ['Is the condition true when the loop is first reached?',
         'i starts above the limit already.',
         'Start below 5.'],
        'i is initialised to 10, so i < 5 is false immediately and the loop never runs. The bug is the initialisation, not the condition.'),

    q('qf_const_false_03', 'CONSTANT_FALSE_LOOP_CONDITION', 'loop_initialization', 'Advanced',
        ['int size = list.size();',
         'for (int i = size; i < size; i++) {',
         '    process(list.get(i));',
         '}'],
        1,
        ['for (int i = 0; i < size; i++) {'],
        ['Compare the start value with the limit.',
         'i == size on the first test, so i < size is false.',
         'Start from 0.'],
        'Starting at size makes the condition false immediately. The loop looks like a normal traversal but never executes.'),

    /* ── DUPLICATE_IF_ELSE_CONDITION / conditional_logic ────────────────── */
    q('qf_dup_cond_01', 'DUPLICATE_IF_ELSE_CONDITION', 'conditional_logic', 'Beginner',
        ['if (n > 0) {',
         '    System.out.println("positive");',
         '} else if (n > 0) {',
         '    System.out.println("negative");',
         '}'],
        2,
        ['} else if (n < 0) {'],
        ['Both branches test the same thing.',
         'The second can never be reached.',
         'The else-if should test the opposite.'],
        'The duplicated condition makes the second branch unreachable, so "negative" is never printed.'),

    q('qf_dup_cond_02', 'DUPLICATE_IF_ELSE_CONDITION', 'conditional_logic', 'Intermediate',
        ['if (score >= 75) {',
         '    grade = "A";',
         '} else if (score >= 75) {',
         '    grade = "B";',
         '} else {',
         '    grade = "C";',
         '}'],
        2,
        ['} else if (score >= 50) {'],
        ['What score would ever produce "B"?',
         'The second test repeats the first.',
         'It should use a lower threshold.'],
        'A grade of "B" is unreachable. Ladder conditions must descend.'),

    q('qf_dup_cond_03', 'DUPLICATE_IF_ELSE_CONDITION', 'conditional_logic', 'Advanced',
        ['if (type.equals("admin")) {',
         '    allow();',
         '} else if (!type.equals("admin")) {',
         '    deny();',
         '} else {',
         '    log("unknown");',
         '}'],
        4,
        ['}'],
        ['The first two branches already cover every case.',
         'The else can never be reached.',
         'Remove the dead else branch.'],
        'A condition and its exact negation are exhaustive, so the else is dead code. Removing it is the honest fix.'),

    /* ── WHILE_VARIABLE_NOT_UPDATED / loop_termination ──────────────────── */
    q('qf_while_var_01', 'WHILE_VARIABLE_NOT_UPDATED', 'loop_termination', 'Beginner',
        ['int i = 0;',
         'while (i < 5) {',
         '    System.out.println(i);',
         '}'],
        2,
        ['System.out.println(i); i++;'],
        ['Does anything inside the loop change i?',
         'The condition can never become false.',
         'Increment i in the body.'],
        'Nothing updates i, so i < 5 stays true forever.'),

    q('qf_while_var_02', 'WHILE_VARIABLE_NOT_UPDATED', 'loop_termination', 'Intermediate',
        ['int remaining = 10;',
         'while (remaining > 0) {',
         '    process();',
         '    int remaining2 = remaining - 1;',
         '}'],
        3,
        ['remaining = remaining - 1;',
         'remaining--;',
         'remaining -= 1;'],
        ['Which variable does the condition test?',
         'The new variable is discarded each iteration.',
         'Update remaining itself.'],
        'The decrement lands in a fresh local variable, so the one the condition tests never changes.'),

    q('qf_while_var_03', 'WHILE_VARIABLE_NOT_UPDATED', 'loop_termination', 'Advanced',
        ['String line = reader.readLine();',
         'while (line != null) {',
         '    System.out.println(line);',
         '    reader.readLine();',
         '}'],
        3,
        ['line = reader.readLine();'],
        ['The next line is read but not stored.',
         'line keeps its first value forever.',
         'Assign the result to line.'],
        'The result of readLine() is discarded inside the loop, so line never becomes null and the first line prints forever.'),

    /* ══════════════════════════════════════════════════════════════════════
       Elementary and Expert.

       Added with the move to five levels. The other three levels came from a
       three-level bank, so these two are the only ones authored WITH their
       level in mind rather than mapped onto it - Elementary is a step past the
       simplest presentation of the same mistake, Expert is where the bug does
       not announce itself and the student has to reason about behaviour rather
       than spot a shape.
       ══════════════════════════════════════════════════════════════════════ */

    q('qf_off_by_one_04', 'OFF_BY_ONE_LOOP_BOUNDARY', 'loop_boundaries', 'Elementary',
        ['int[] scores = new int[5];',
         'for (int i = 1; i <= 5; i++) {',
         '    scores[i] = i * 10;',
         '}'],
        1,
        ['for (int i = 0; i < 5; i++) {',
         'for (int i = 0; i <= 4; i++) {'],
        ['The array has 5 slots. What are their indices?',
         'Writing to scores[5] is out of bounds.',
         'Run i from 0 to 4.'],
        'Counting 1..5 writes to scores[5], which does not exist, and never writes scores[0].'),

    q('qf_off_by_one_05', 'OFF_BY_ONE_LOOP_BOUNDARY', 'loop_boundaries', 'Expert',
        ['public static boolean isPalindrome(char[] c) {',
         '    for (int i = 0; i <= c.length / 2; i++) {',
         '        if (c[i] != c[c.length - 1 - i]) return false;',
         '    }',
         '    return true;',
         '}'],
        1,
        ['for (int i = 0; i < c.length / 2; i++) {'],
        ['Work through an array of length 4. Which pairs get compared?',
         'At i == length/2 the two indices cross over each other.',
         'The loop should stop before length / 2.'],
        'With <=, the loop compares past the midpoint and re-checks a pair from the other side. It still returns the right answer for palindromes, which is what makes it hard to catch - the bug is wasted work and, for odd lengths, comparing an element with itself.'),

    q('qf_cond_op_04', 'INCORRECT_CONDITIONAL_OPERATOR', 'conditional_logic', 'Elementary',
        ['int stock = 0;',
         'if (stock > 0) {',
         '    System.out.println("In stock");',
         '} else if (stock < 0) {',
         '    System.out.println("Out of stock");',
         '}'],
        3,
        ['} else {'],
        ['What is printed when stock is exactly 0?',
         'Neither branch matches zero.',
         'The second branch should catch everything else.'],
        'Zero falls through both conditions, so nothing prints for the one case that matters most.'),

    q('qf_cond_op_05', 'INCORRECT_CONDITIONAL_OPERATOR', 'conditional_logic', 'Expert',
        ['public static String size(double kg) {',
         '    if (kg < 1.0) return "small";',
         '    if (kg < 10.0) return "medium";',
         '    if (kg > 10.0) return "large";',
         '    return "unknown";',
         '}'],
        3,
        ['return "large";',
         'if (kg >= 10.0) return "large";'],
        ['What does size(10.0) return?',
         'Exactly 10.0 fails every condition.',
         'The last case should be unconditional, or use >=.'],
        'The boundary value 10.0 is not less than 10.0 and not greater than 10.0, so it falls to "unknown". Strict inequalities on both sides of a boundary always leave a hole.'),

    q('qf_arr_len_04', 'ARRAY_LENGTH_INDEX_MISUSE', 'array_indexing', 'Elementary',
        ['int[] data = {5, 7, 9};',
         'int last = data.length;',
         'System.out.println(data[last]);'],
        1,
        ['int last = data.length - 1;'],
        ['length counts elements; indices start at zero.',
         'The last index is one below the length.',
         'Subtract one when computing last.'],
        'last holds 3, but the highest valid index is 2. Naming the variable "last" makes the mistake read as correct.'),

    q('qf_arr_len_05', 'ARRAY_LENGTH_INDEX_MISUSE', 'array_indexing', 'Expert',
        ['public static void shiftLeft(int[] a) {',
         '    for (int i = 0; i < a.length; i++) {',
         '        a[i] = a[i + 1];',
         '    }',
         '    a[a.length - 1] = 0;',
         '}'],
        1,
        ['for (int i = 0; i < a.length - 1; i++) {'],
        ['The body reads one element ahead of i.',
         'On the final iteration it reads a[a.length].',
         'The loop must stop one short.'],
        'Reading a[i + 1] means the loop bound has to account for the lookahead. The trailing assignment already handles the last slot, which is what makes the off-by-one easy to miss.'),

    q('qf_str_eq_04', 'STRING_EQUALITY_WITH_OPERATOR', 'string_comparison', 'Elementary',
        ['String role = getRole();',
         'if (role == null || role == "") {',
         '    System.out.println("no role");',
         '}'],
        1,
        ['if (role == null || role.isEmpty()) {',
         'if (role == null || "".equals(role)) {'],
        ['One of these two comparisons is fine. Which?',
         '== null is correct; == "" is not.',
         'Test emptiness with isEmpty() or "".equals().'],
        'Comparing against null with == is correct and necessary; comparing against a string literal with == is not. The line mixes a right and a wrong use of the same operator.'),

    q('qf_str_eq_05x', 'STRING_EQUALITY_WITH_OPERATOR', 'string_comparison', 'Expert',
        ['String a = "code";',
         'String b = "code";',
         'String c = new String("code");',
         'System.out.println((a == b) + " " + (a == c));'],
        3,
        ['System.out.println(a.equals(b) + " " + a.equals(c));'],
        ['a == b prints true and a == c prints false, for the same text.',
         'Compile-time literals are pooled; new String() is not.',
         'Use .equals() for both.'],
        'a == b is true only because both are pooled literals - an accident of compilation, not a property of the values. Relying on it produces code that works until a string arrives at runtime.'),

    q('qf_loop_dir_04', 'LOOP_UPDATE_WRONG_DIRECTION', 'loop_control', 'Elementary',
        ['int i = 0;',
         'while (i != 10) {',
         '    System.out.println(i);',
         '    i += 3;',
         '}'],
        1,
        ['while (i < 10) {'],
        ['i takes the values 0, 3, 6, 9, 12...',
         'It steps straight over 10.',
         'Test with < rather than !=.'],
        'A step of 3 never lands exactly on 10, so != never becomes false. Inequality is safer than equality for a loop bound whenever the step is not 1.'),

    q('qf_loop_dir_05', 'LOOP_UPDATE_WRONG_DIRECTION', 'loop_control', 'Expert',
        ['for (int i = 0; i < list.size(); i++) {',
         '    if (list.get(i).isEmpty()) {',
         '        list.remove(i);',
         '    }',
         '}'],
        2,
        ['list.remove(i--);',
         'list.remove(i); i--;'],
        ['What happens to the element after one that is removed?',
         'Removing shifts everything left, but i still advances.',
         'Step i back when an element is removed.'],
        'Removing at i shifts the next element into position i, and then i++ skips it. Two adjacent empty strings mean the second survives.'),

    q('qf_unreach_04', 'UNREACHABLE_CODE_AFTER_RETURN', 'control_flow', 'Elementary',
        ['public static void report(int n) {',
         '    if (n < 0) {',
         '        return;',
         '    }',
         '    return;',
         '    System.out.println(n);',
         '}'],
        4,
        ['}'],
        ['The second return ends the method for every input.',
         'The println below can never run.',
         'Remove the stray return and close the if.'],
        'The unconditional return makes the rest of the method dead. The first return is legitimate; the second is not.'),

    q('qf_unreach_05', 'UNREACHABLE_CODE_AFTER_RETURN', 'control_flow', 'Expert',
        ['public static int total(int[] a) {',
         '    int sum = 0;',
         '    for (int n : a) {',
         '        sum += n;',
         '        return sum;',
         '    }',
         '    return sum;',
         '}'],
        4,
        ['}'],
        ['How many elements does this actually add up?',
         'The return is inside the loop.',
         'Remove it and let the loop finish.'],
        'The return sits inside the loop body, so the method returns after the first element. It compiles, runs and returns a plausible number - which is why this one survives testing on single-element arrays.'),

    q('qf_switch_04', 'MISSING_BREAK_IN_SWITCH', 'switch_statements', 'Elementary',
        ['switch (size) {',
         '    case "S": price = 5; break;',
         '    case "M": price = 7;',
         '    case "L": price = 9; break;',
         '    default: price = 0;',
         '}'],
        2,
        ['case "M": price = 7; break;'],
        ['What price does an "M" end up with?',
         'Case M falls through into case L.',
         'Break after setting 7.'],
        'A medium is charged 9 because case "M" falls through. Only one case is missing its break, which makes it easy to skim past.'),

    q('qf_switch_05', 'MISSING_BREAK_IN_SWITCH', 'switch_statements', 'Expert',
        ['switch (level) {',
         '    case 3:',
         '    case 2:',
         '        grantAdmin();',
         '    case 1:',
         '        grantRead();',
         '        break;',
         '}'],
        3,
        ['grantAdmin(); break;'],
        ['Some fall-through here is deliberate. Which?',
         'Cases 3 and 2 sharing a body is intentional; falling into case 1 is not.',
         'Break after grantAdmin().'],
        'Stacked labels (case 3: case 2:) are a legitimate idiom for sharing a body, so not every fall-through is a bug. The one after grantAdmin() is - it grants read access to levels that should not receive it by that path.'),

    q('qf_empty_if_04', 'EMPTY_CONDITIONAL_BODY', 'statement_structure', 'Elementary',
        ['while (queue.hasNext());',
         '{',
         '    process(queue.next());',
         '}'],
        0,
        ['while (queue.hasNext())',
         'while (queue.hasNext()) {'],
        ['The semicolon makes the loop body empty.',
         'The condition never changes, so it spins forever.',
         'Remove the semicolon.'],
        'An empty body plus an unchanging condition is an infinite loop that never reaches the block below it.'),

    q('qf_empty_if_05', 'EMPTY_CONDITIONAL_BODY', 'statement_structure', 'Expert',
        ['if (a > b)',
         '    if (b > c)',
         '        System.out.println("a > b > c");',
         'else',
         '    System.out.println("a <= b");'],
        3,
        ['} else {',
         'else if (a <= b)'],
        ['Which if does the else belong to?',
         'An else binds to the nearest unmatched if, not the one it lines up with.',
         'Braces are needed to say which is meant.'],
        'The dangling else binds to the inner if, not the outer one as the indentation suggests, so "a <= b" prints when a > b and b <= c. Indentation is not syntax.'),

    q('qf_self_assign_04', 'SELF_ASSIGNMENT', 'assignment_logic', 'Elementary',
        ['int width = 10;',
         'int height = 4;',
         'width = width;',
         'int area = width * height;'],
        2,
        ['width = width * 2;',
         'width = height;'],
        ['This line has no effect at all.',
         'Assigning a variable to itself changes nothing.',
         'It should assign something else.'],
        'A self-assignment compiles and runs and does nothing, so the intended change is silently missing.'),

    q('qf_self_assign_05', 'SELF_ASSIGNMENT', 'assignment_logic', 'Expert',
        ['public void update(Point other) {',
         '    this.x = this.x;',
         '    this.y = other.y;',
         '}'],
        1,
        ['this.x = other.x;'],
        ['Compare the two lines. Only one reads from `other`.',
         'The first line assigns the field to itself.',
         'It should read other.x.'],
        'Half the update is missing. The second line shows what the first was meant to be, and the symmetry is what makes the eye skip over it.'),

    q('qf_bool_or_04', 'ALWAYS_TRUE_OR_CONDITION', 'boolean_logic', 'Elementary',
        ['if (name != null || name.length() > 0) {',
         '    System.out.println(name);',
         '}'],
        0,
        ['if (name != null && name.length() > 0) {'],
        ['What happens when name is null?',
         'With ||, the second half still runs and throws.',
         'Use && so the null check guards the call.'],
        'With ||, a null name passes the first test and then the second half dereferences it. && short-circuits, which is exactly what a null guard needs.'),

    q('qf_bool_or_05', 'ALWAYS_TRUE_OR_CONDITION', 'boolean_logic', 'Expert',
        ['public static boolean isOutsideRange(int n, int lo, int hi) {',
         '    return !(n >= lo) || !(n <= hi) || lo > hi;',
         '}'],
        1,
        ['return !(n >= lo) || !(n <= hi);',
         'return n < lo || n > hi;'],
        ['The third term does not describe n at all.',
         'It reports "outside range" for every n when the caller passes a bad range.',
         'Drop it.'],
        'The third condition mixes an argument-validation concern into a predicate about n, so a caller with lo > hi is told every value is outside the range rather than being told their arguments are wrong.'),

    q('qf_immut_04', 'IGNORED_STRING_METHOD_RESULT', 'immutable_strings', 'Elementary',
        ['String path = "a/b/c";',
         'String[] parts = path.split("/");',
         'path.toUpperCase();',
         'System.out.println(path + " " + parts.length);'],
        2,
        ['path = path.toUpperCase();'],
        ['split() is used correctly. The next line is not.',
         'toUpperCase() returns a new String.',
         'Assign it back to path.'],
        'The line above shows the correct pattern - the result of split() is assigned. The next line makes the same kind of call and throws the result away.'),

    q('qf_immut_05', 'IGNORED_STRING_METHOD_RESULT', 'immutable_strings', 'Expert',
        ['StringBuilder sb = new StringBuilder("ab");',
         'sb.append("c");',
         'String s = "ab";',
         's.concat("c");',
         'System.out.println(sb + " " + s);'],
        3,
        ['s = s.concat("c");',
         's += "c";'],
        ['Two lines do the same thing. Only one works.',
         'StringBuilder mutates; String does not.',
         'Assign the result of concat back to s.'],
        'The StringBuilder line is correct precisely because StringBuilder is mutable. The String line looks identical and is not, which is the distinction this question is about.'),

    q('qf_div_zero_04', 'DIVISION_BY_ZERO_LITERAL', 'arithmetic_operations', 'Elementary',
        ['int items = 7;',
         'int perBox = 0;',
         'int boxes = items / perBox;'],
        2,
        ['int boxes = items / 1;'],
        ['perBox is zero on the line above.',
         'Integer division by zero throws.',
         'The divisor must be non-zero.'],
        'The zero is in a variable rather than written on the dividing line, which is the only difference from the simpler version of this bug.'),

    q('qf_div_zero_05', 'DIVISION_BY_ZERO_LITERAL', 'arithmetic_operations', 'Expert',
        ['public static double share(int part, int whole) {',
         '    if (whole != 0) {',
         '        return part / whole;',
         '    }',
         '    return 0;',
         '}'],
        2,
        ['return (double) part / whole;',
         'return part / (double) whole;'],
        ['The zero guard is correct. The arithmetic is not.',
         'Two ints divide as integers, then widen to double.',
         'Cast one operand before dividing.'],
        'share(1, 2) returns 0.0, not 0.5: the division happens in integer arithmetic and the result is widened afterwards. The guard against division by zero is right, which draws the eye away from the real defect.'),

    q('qf_const_false_04', 'CONSTANT_FALSE_LOOP_CONDITION', 'loop_initialization', 'Elementary',
        ['int count = 0;',
         'int limit = 0;',
         'while (count < limit) {',
         '    count++;',
         '}'],
        1,
        ['int limit = 10;'],
        ['Is the condition ever true?',
         'limit is zero, so count < limit fails immediately.',
         'Give limit a positive value.'],
        'The loop is well formed but its limit is zero, so the body never runs. Nothing about the loop itself is wrong.'),

    q('qf_const_false_05', 'CONSTANT_FALSE_LOOP_CONDITION', 'loop_initialization', 'Expert',
        ['int found = -1;',
         'for (int i = 0; i < arr.length && found >= 0; i++) {',
         '    if (arr[i] == target) found = i;',
         '}'],
        1,
        ['for (int i = 0; i < arr.length && found < 0; i++) {'],
        ['found starts at -1. Is the second condition true then?',
         'The loop stops before it starts, so found is never set.',
         'The condition is inverted - continue WHILE not found.'],
        'The guard is backwards: it demands the value already be found in order to look for it. The loop never executes and the method always reports -1.'),

    q('qf_dup_cond_04', 'DUPLICATE_IF_ELSE_CONDITION', 'conditional_logic', 'Elementary',
        ['if (temp > 30) {',
         '    System.out.println("hot");',
         '} else if (temp > 30) {',
         '    System.out.println("warm");',
         '} else {',
         '    System.out.println("cold");',
         '}'],
        2,
        ['} else if (temp > 20) {'],
        ['When would "warm" ever print?',
         'The second test repeats the first exactly.',
         'It should use a lower threshold.'],
        '"warm" is unreachable. A condition ladder has to descend for each branch to be reachable.'),

    q('qf_dup_cond_05', 'DUPLICATE_IF_ELSE_CONDITION', 'conditional_logic', 'Expert',
        ['if (n % 2 == 0) {',
         '    System.out.println("even");',
         '} else if (n % 2 != 1) {',
         '    System.out.println("odd");',
         '}'],
        2,
        ['} else {'],
        ['For positive n these two conditions are the same test.',
         'If n % 2 is not 0 then for positive n it is 1.',
         'The second branch should just be else.'],
        'The two conditions are the same claim written differently, so "odd" never prints for positive n. It is not a literal duplicate, which is what makes it harder than the obvious version - and for negative n, n % 2 is -1 in Java, so the second condition is true and the branch prints "odd" for exactly the inputs the author was not thinking about.'),

    q('qf_while_var_04', 'WHILE_VARIABLE_NOT_UPDATED', 'loop_termination', 'Elementary',
        ['int attempts = 0;',
         'while (attempts < 3) {',
         '    tryConnect();',
         '    if (isConnected()) break;',
         '}'],
        3,
        ['if (isConnected()) break; attempts++;',
         'attempts++; if (isConnected()) break;'],
        ['What happens if the connection never succeeds?',
         'Nothing increments attempts.',
         'Count the attempt inside the loop.'],
        'The break gives the loop one way out, which hides the missing increment: if the connection never comes up, it retries forever rather than three times.'),

    q('qf_while_var_05', 'WHILE_VARIABLE_NOT_UPDATED', 'loop_termination', 'Expert',
        ['int n = 27;',
         'int steps = 0;',
         'while (n != 1) {',
         '    n = (n % 2 == 0) ? n / 2 : 3 * n + 1;',
         '    steps = steps;',
         '}'],
        4,
        ['steps++;',
         'steps = steps + 1;'],
        ['The loop does terminate. Something else is broken.',
         'steps is assigned to itself every iteration.',
         'Increment it.'],
        'The loop variable n is updated correctly and the loop ends. It is the counter that never moves, so the method reports 0 steps for a sequence that took over a hundred.'),
];

const seed = async () => {
    await mongoose.connect(process.env.MONGODB_URI);

    let written = 0;
    for (const question of QUESTIONS) {
        await QuestionBank.updateOne({ id: question.id }, { $set: question }, { upsert: true });
        written += 1;
    }

    const total = await QuestionBank.countDocuments({ gameType: 'CodeFix' });
    const concepts = await QuestionBank.distinct('conceptTag', { gameType: 'CodeFix' });

    console.log(`Upserted ${written} CodeFix questions.`);
    console.log(`Bank now holds ${total} CodeFix questions across ${concepts.length} concepts.`);

    await mongoose.disconnect();
};

seed().catch((error) => {
    console.error(error.message);
    process.exit(1);
});
