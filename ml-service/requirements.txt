Flask==3.0.2
scikit-learn==1.4.1.post1
pandas==2.2.1
numpy==1.26.4
joblib==1.3.2
flask-cors==4.0.0
python-dotenv==1.0.1

# Needed by retrain_from_db.py, which trains model.pkl from the game sessions in
# MongoDB. Neither was declared, so the retrain script could not run at all -
# and because model.pkl is gitignored and never ships, /predict answered
# "Model not trained yet" and the backend silently fell back to a hand-written
# heuristic. The Random Forest was never actually running.
pymongo>=4.6
certifi>=2024.2.2

# Production WSGI server. `flask run` / app.run() is the development server and
# says so on startup; it is single-threaded by default and not written to face
# a network. The Dockerfile serves through gunicorn instead.
gunicorn==21.2.0
